#include <stdio.h>
int main() {
  int a, b, c;
  int row, col;
  printf("Enter the number of rows and col :");
  scanf("%d %d", &row, &col);
  int i=1,j=1;
  while(i<=3)
  {
      while()
  }
  
  return 0;
}
#include <stdio.h>
int main()
{
int a,b,c,max;
printf("Enter the number of a b c");
scanf("%d %d %d",&a,&b,&c);
if(a>b && a>c)
{
    printf("A is bigger%d\n",a);
}
else if (b>c && b>a)

return 0;
}
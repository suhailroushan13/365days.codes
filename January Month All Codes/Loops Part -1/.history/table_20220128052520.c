#include <stdio.h>
int main()
{
int a,b,c;
int num,till;

printf("Enter a number : ");
scanf("%d",&num);
for(num;num<10;num++) 
{
    last = num*n
    printf("%d X %d = %d\n",num,num,num*num);
}

return 0;
}